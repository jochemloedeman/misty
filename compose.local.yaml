services:
  app:
    build: .
    image: misty-app-local
    ports:
      - "8080:8080"
    environment:
      APNS_DEVELOPMENT: "true"
      REFRESH_INTERVAL: ${REFRESH_INTERVAL:-}
      NOTIFY_INTERVAL: ${NOTIFY_INTERVAL:-}
      APNS_SIMULATE_STATUS: ${APNS_SIMULATE_STATUS:-}

  db:
    ports:
      - "5432:5432"

  caddy:
    profiles: [disabled]

  db-backup:
    profiles: [disabled]

  analytics-init:
    build: ./analytics
    image: misty-analytics-local

  analytics:
    build: ./analytics
    image: misty-analytics-local

  analytics-mcp:
    build: ./analytics
    image: misty-analytics-local
    ports:
      - "8001:8000"

secrets:
  rclone_conf:
    file: /dev/null
  cloudflare_api_token:
    file: /dev/null
  aop_ca_cert:
    file: /dev/null
