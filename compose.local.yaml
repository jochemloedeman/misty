services:
  app:
    build: .
    image: misty-app-local
    ports:
      - "8080:8080"

  db:
    ports:
      - "5432:5432"

  caddy:
    profiles: [disabled]

secrets:
  cloudflare_api_token:
    file: /dev/null
  aop_ca_cert:
    file: /dev/null
